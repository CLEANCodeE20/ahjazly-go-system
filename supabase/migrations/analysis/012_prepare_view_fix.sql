-- ============================================
-- الاستراتيجية الأفضل: فك الارتباط ثم الحذف
-- ============================================

-- 1. معرفة تعريف الـ View الحالي (للاحتفاظ به وتعديله)
SELECT definition 
FROM pg_views 
WHERE viewname = 'v_available_trips';

-- 2. سنقوم في الخطوة التالية بإعادة إنشاء الـ View 
-- بحيث يستبدل "is_available" بـ "TRUE" (قيمة ثابتة مؤقتة)
-- ثم نحذف العمود الأصلي بأمان.
