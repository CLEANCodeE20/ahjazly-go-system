# سكريبتات التحليل والتحضير - المرحلة 0

## 📋 نظرة عامة

هذا المجلد يحتوي على سكريبتات **آمنة (قراءة فقط)** لتحليل قاعدة البيانات قبل البدء بأي تعديلات.

## 📁 الملفات

### 1. `000_comprehensive_analysis.sql`
**الوصف:** سكريبت تحليل شامل لقاعدة البيانات  
**الأمان:** ✅ قراءة فقط - لا يُجري أي تعديلات  
**المدة:** 2-5 دقائق  

**ما يفحصه:**
- ✅ معلومات عامة (حجم، عدد الجداول، الفهارس، القيود)
- ✅ أكبر 10 جداول
- ✅ البيانات اليتيمة (10 أنواع)
- ✅ التكرارات (5 أنواع)
- ✅ القيم غير الصالحة (6 أنواع)
- ✅ القيود الموجودة (FK, UK, CK, PK)
- ✅ الفهارس غير المستخدمة
- ✅ حالة RLS
- ✅ ملخص شامل

### 2. `001_backup_script.sh`
**الوصف:** سكريبت النسخ الاحتياطي الكامل  
**الأمان:** ✅ آمن - ينشئ نسخة احتياطية فقط  
**المدة:** 5-15 دقيقة (حسب حجم البيانات)

---

## 🚀 كيفية الاستخدام

### الخطوة 1: تشغيل التحليل الشامل

#### على Windows (PowerShell):
```powershell
# الاتصال بقاعدة البيانات وتشغيل التحليل
psql -h [your-host] -U [your-user] -d [your-database] -f analysis/000_comprehensive_analysis.sql > analysis_report.txt 2>&1

# عرض النتائج
cat analysis_report.txt
```

#### على Supabase:
```bash
# استخدم Supabase CLI
supabase db execute --file supabase/migrations/analysis/000_comprehensive_analysis.sql

# أو من SQL Editor في Dashboard
# انسخ محتوى الملف والصقه في SQL Editor
```

### الخطوة 2: مراجعة النتائج

افتح ملف `analysis_report.txt` وراجع:

1. **القسم 3 (البيانات اليتيمة):**
   - إذا كان العدد > 0، ستحتاج للتنظيف
   - البيانات المالية (مدفوعات، عمولات) تحتاج مراجعة يدوية

2. **القسم 4 (التكرارات):**
   - أي تكرارات يجب حلها قبل إضافة Unique Constraints

3. **القسم 5 (القيم غير الصالحة):**
   - يجب إصلاحها قبل إضافة Check Constraints

4. **القسم 6 (القيود):**
   - إذا كان عدد Foreign Keys < 30، تحتاج المرحلة 1

5. **القسم 8 (RLS):**
   - أي جدول بدون RLS يحتاج المرحلة 3

### الخطوة 3: إنشاء نسخة احتياطية

```bash
# على Windows
bash analysis/001_backup_script.sh

# على Linux/Mac
./analysis/001_backup_script.sh
```

---

## 📊 فهم النتائج

### مثال على نتيجة جيدة:
```
إجمالي البيانات اليتيمة: 0
إجمالي التكرارات: 0
إجمالي القيم غير الصالحة: 0
عدد Foreign Keys الموجودة: 52
جداول بدون RLS: 0
```
**الحالة:** ✅ جاهز للمرحلة 1 مباشرة

### مثال على نتيجة تحتاج عمل:
```
إجمالي البيانات اليتيمة: 145
إجمالي التكرارات: 23
إجمالي القيم غير الصالحة: 8
عدد Foreign Keys الموجودة: 5
جداول بدون RLS: 6
```
**الحالة:** ⚠️ يحتاج المرحلة 0.5 (تنظيف البيانات) أولاً

---

## ⚠️ تحذيرات مهمة

1. **لا تشغل هذه السكريبتات على الإنتاج مباشرة** إذا كان النظام تحت حمل عالي
2. **السكريبتات آمنة** - لا تُجري أي تعديلات، فقط قراءة
3. **احفظ النتائج** - ستحتاجها للمقارنة لاحقاً
4. **راجع البيانات المالية يدوياً** - لا تحذف أي بيانات مالية تلقائياً

---

## 📈 الخطوات التالية

بناءً على نتائج التحليل:

### إذا كانت النتائج نظيفة (0 مشاكل):
1. ✅ انتقل مباشرة للمرحلة 1 (إضافة Foreign Keys)
2. ✅ راجع `خطة_التحسين_الشاملة.md` للتفاصيل

### إذا كانت هناك مشاكل:
1. ⚠️ ابدأ بالمرحلة 0.5 (تنظيف البيانات)
2. ⚠️ راجع `المرحلة_0.5_تنظيف_البيانات.md`
3. ⚠️ نفذ سكريبتات التنظيف
4. ⚠️ أعد تشغيل التحليل للتأكد

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. تأكد من صلاحيات الاتصال بقاعدة البيانات
2. تأكد من وجود جميع الجداول المطلوبة
3. راجع سجلات الأخطاء في `analysis_report.txt`

---

## 📝 ملاحظات

- **تاريخ الإنشاء:** 2026-01-30
- **الإصدار:** 1.0
- **الحالة:** جاهز للاستخدام
