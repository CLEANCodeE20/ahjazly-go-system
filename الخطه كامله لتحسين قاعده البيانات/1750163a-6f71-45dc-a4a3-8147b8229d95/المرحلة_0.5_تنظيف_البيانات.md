# المرحلة 0.5: تنظيف البيانات
**المدة:** أسبوع واحد (بين المرحلة 0 والمرحلة 1)  
**الأولوية:** 🔴 حرجة - يجب تنفيذها قبل إضافة Foreign Keys

---

## الهدف

تنظيف وإصلاح البيانات الحالية لضمان نجاح إضافة قيود المفاتيح الأجنبية بدون أخطاء.

---

## 1. تحليل البيانات اليتيمة

### 1.1 اكتشاف البيانات اليتيمة

```sql
-- ملف: analysis/001_detect_orphaned_data.sql

-- 1. الركاب اليتامى (بدون حجوزات)
SELECT 
  'Orphaned Passengers' as issue,
  COUNT(*) as count,
  'CRITICAL' as severity
FROM passengers p
LEFT JOIN bookings b ON p.booking_id = b.booking_id
WHERE b.booking_id IS NULL;

-- 2. الحجوزات اليتيمة (بدون مستخدمين)
SELECT 
  'Orphaned Bookings (No User)' as issue,
  COUNT(*) as count,
  'CRITICAL' as severity
FROM bookings b
LEFT JOIN users u ON b.user_id = u.user_id
WHERE u.user_id IS NULL;

-- 3. الحجوزات اليتيمة (بدون رحلات)
SELECT 
  'Orphaned Bookings (No Trip)' as issue,
  COUNT(*) as count,
  'CRITICAL' as severity
FROM bookings b
LEFT JOIN trips t ON b.trip_id = t.trip_id
WHERE t.trip_id IS NULL;

-- 4. الرحلات اليتيمة (بدون مسارات)
SELECT 
  'Orphaned Trips (No Route)' as issue,
  COUNT(*) as count,
  'HIGH' as severity
FROM trips t
LEFT JOIN routes r ON t.route_id = r.route_id
WHERE r.route_id IS NULL;

-- 5. الرحلات اليتيمة (بدون حافلات)
SELECT 
  'Orphaned Trips (No Bus)' as issue,
  COUNT(*) as count,
  'HIGH' as severity
FROM trips t
LEFT JOIN buses b ON t.bus_id = b.bus_id
WHERE t.bus_id IS NOT NULL AND b.bus_id IS NULL;

-- 6. السائقون اليتامى (بدون شركاء)
SELECT 
  'Orphaned Drivers' as issue,
  COUNT(*) as count,
  'MEDIUM' as severity
FROM drivers d
LEFT JOIN partners p ON d.partner_id = p.partner_id
WHERE d.partner_id IS NOT NULL AND p.partner_id IS NULL;

-- 7. الحافلات اليتيمة (بدون شركاء)
SELECT 
  'Orphaned Buses' as issue,
  COUNT(*) as count,
  'MEDIUM' as severity
FROM buses b
LEFT JOIN partners p ON b.partner_id = p.partner_id
WHERE b.partner_id IS NOT NULL AND p.partner_id IS NULL;

-- 8. المقاعد اليتيمة (بدون حافلات)
SELECT 
  'Orphaned Seats' as issue,
  COUNT(*) as count,
  'MEDIUM' as severity
FROM seats s
LEFT JOIN buses b ON s.bus_id = b.bus_id
WHERE s.bus_id IS NOT NULL AND b.bus_id IS NULL;

-- 9. المدفوعات اليتيمة (بدون حجوزات)
SELECT 
  'Orphaned Payments' as issue,
  COUNT(*) as count,
  'CRITICAL' as severity
FROM payment_transactions pt
LEFT JOIN bookings b ON pt.booking_id = b.booking_id
WHERE pt.booking_id IS NOT NULL AND b.booking_id IS NULL;

-- 10. العمولات اليتيمة (بدون حجوزات)
SELECT 
  'Orphaned Commissions' as issue,
  COUNT(*) as count,
  'HIGH' as severity
FROM commissions c
LEFT JOIN bookings b ON c.booking_id = b.booking_id
WHERE c.booking_id IS NOT NULL AND b.booking_id IS NULL;
```

### 1.2 حفظ نتائج التحليل

```sql
-- إنشاء جدول لتوثيق البيانات اليتيمة
CREATE TABLE IF NOT EXISTS data_cleanup_log (
  log_id SERIAL PRIMARY KEY,
  issue_type VARCHAR(100),
  records_count INTEGER,
  severity VARCHAR(20),
  action_taken TEXT,
  deleted_records INTEGER DEFAULT 0,
  fixed_records INTEGER DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- حفظ النتائج
INSERT INTO data_cleanup_log (issue_type, records_count, severity)
SELECT 'Orphaned Passengers', COUNT(*), 'CRITICAL'
FROM passengers p LEFT JOIN bookings b ON p.booking_id = b.booking_id
WHERE b.booking_id IS NULL;

-- كرر لكل نوع من البيانات اليتيمة...
```

---

## 2. استراتيجية التنظيف

### 2.1 قواعد القرار

| نوع البيانات | الإجراء | السبب |
|--------------|---------|--------|
| ركاب يتامى | **حذف** | لا يمكن أن يوجد راكب بدون حجز |
| حجوزات بدون مستخدم | **تحقيق يدوي** | قد تكون بيانات مالية مهمة |
| حجوزات بدون رحلة | **تحقيق يدوي** | قد تكون رحلات محذوفة خطأً |
| رحلات بدون مسار | **إصلاح أو حذف** | حسب الحالة |
| مدفوعات يتيمة | **تحقيق يدوي** | بيانات مالية حرجة |
| عمولات يتيمة | **حذف** | يمكن إعادة حسابها |

### 2.2 سكريبتات التنظيف

```sql
-- ملف: cleanup/001_cleanup_orphaned_data.sql

-- تفعيل وضع المعاملات
BEGIN;

-- 1. حذف الركاب اليتامى (بعد التوثيق)
-- أولاً: احفظ نسخة للتدقيق
CREATE TABLE passengers_deleted_backup AS
SELECT p.*, NOW() as deleted_at, 'orphaned - no booking' as reason
FROM passengers p
LEFT JOIN bookings b ON p.booking_id = b.booking_id
WHERE b.booking_id IS NULL;

-- ثانياً: احذف
DELETE FROM passengers
WHERE passenger_id IN (
  SELECT p.passenger_id
  FROM passengers p
  LEFT JOIN bookings b ON p.booking_id = b.booking_id
  WHERE b.booking_id IS NULL
);

-- سجل العملية
INSERT INTO data_cleanup_log (issue_type, action_taken, deleted_records)
VALUES ('Orphaned Passengers', 'Deleted and backed up', 
  (SELECT COUNT(*) FROM passengers_deleted_backup));

-- 2. حذف العمولات اليتيمة
CREATE TABLE commissions_deleted_backup AS
SELECT c.*, NOW() as deleted_at, 'orphaned - no booking' as reason
FROM commissions c
LEFT JOIN bookings b ON c.booking_id = b.booking_id
WHERE c.booking_id IS NOT NULL AND b.booking_id IS NULL;

DELETE FROM commissions
WHERE commission_id IN (
  SELECT c.commission_id
  FROM commissions c
  LEFT JOIN bookings b ON c.booking_id = b.booking_id
  WHERE c.booking_id IS NOT NULL AND b.booking_id IS NULL
);

INSERT INTO data_cleanup_log (issue_type, action_taken, deleted_records)
VALUES ('Orphaned Commissions', 'Deleted and backed up',
  (SELECT COUNT(*) FROM commissions_deleted_backup));

-- 3. حذف المقاعد اليتيمة
CREATE TABLE seats_deleted_backup AS
SELECT s.*, NOW() as deleted_at, 'orphaned - no bus' as reason
FROM seats s
LEFT JOIN buses b ON s.bus_id = b.bus_id
WHERE s.bus_id IS NOT NULL AND b.bus_id IS NULL;

DELETE FROM seats
WHERE seat_id IN (
  SELECT s.seat_id
  FROM seats s
  LEFT JOIN buses b ON s.bus_id = b.bus_id
  WHERE s.bus_id IS NOT NULL AND b.bus_id IS NULL
);

INSERT INTO data_cleanup_log (issue_type, action_taken, deleted_records)
VALUES ('Orphaned Seats', 'Deleted and backed up',
  (SELECT COUNT(*) FROM seats_deleted_backup));

-- 4. للبيانات المالية: تحقيق يدوي فقط
-- لا تحذف تلقائياً - أنشئ تقرير للمراجعة اليدوية
CREATE TABLE financial_data_review AS
SELECT 
  'Orphaned Booking' as type,
  b.booking_id,
  b.user_id,
  b.total_price,
  b.booking_date,
  'User does not exist' as issue
FROM bookings b
LEFT JOIN users u ON b.user_id = u.user_id
WHERE u.user_id IS NULL
UNION ALL
SELECT 
  'Orphaned Payment' as type,
  pt.transaction_id::BIGINT,
  pt.user_id,
  pt.amount,
  pt.created_at,
  'Booking does not exist' as issue
FROM payment_transactions pt
LEFT JOIN bookings b ON pt.booking_id = b.booking_id
WHERE pt.booking_id IS NOT NULL AND b.booking_id IS NULL;

-- سجل للمراجعة
INSERT INTO data_cleanup_log (issue_type, action_taken, records_count)
VALUES ('Financial Data Issues', 'Created review table for manual inspection',
  (SELECT COUNT(*) FROM financial_data_review));

COMMIT;
```

---

## 3. التحقق من التكرارات

### 3.1 اكتشاف التكرارات

```sql
-- ملف: analysis/002_detect_duplicates.sql

-- 1. رخص سائقين مكررة
SELECT 
  license_number,
  COUNT(*) as duplicate_count,
  STRING_AGG(driver_id::TEXT, ', ') as driver_ids
FROM drivers
WHERE license_number IS NOT NULL
GROUP BY license_number
HAVING COUNT(*) > 1;

-- 2. لوحات حافلات مكررة
SELECT 
  license_plate,
  COUNT(*) as duplicate_count,
  STRING_AGG(bus_id::TEXT, ', ') as bus_ids
FROM buses
WHERE license_plate IS NOT NULL
GROUP BY license_plate
HAVING COUNT(*) > 1;

-- 3. مقاعد مكررة لنفس الحافلة
SELECT 
  bus_id,
  seat_number,
  COUNT(*) as duplicate_count,
  STRING_AGG(seat_id::TEXT, ', ') as seat_ids
FROM seats
GROUP BY bus_id, seat_number
HAVING COUNT(*) > 1;

-- 4. أرقام فواتير مكررة
SELECT 
  invoice_number,
  COUNT(*) as duplicate_count,
  STRING_AGG(invoice_id::TEXT, ', ') as invoice_ids
FROM partner_invoices
WHERE invoice_number IS NOT NULL
GROUP BY invoice_number
HAVING COUNT(*) > 1;

-- 5. مستخدمين بنفس auth_id
SELECT 
  auth_id,
  COUNT(*) as duplicate_count,
  STRING_AGG(user_id::TEXT, ', ') as user_ids
FROM users
WHERE auth_id IS NOT NULL
GROUP BY auth_id
HAVING COUNT(*) > 1;
```

### 3.2 حل التكرارات

```sql
-- ملف: cleanup/002_resolve_duplicates.sql

BEGIN;

-- 1. حل تكرار رخص السائقين
-- استراتيجية: احتفظ بالأحدث، احذف القديم
WITH duplicates AS (
  SELECT 
    license_number,
    driver_id,
    created_at,
    ROW_NUMBER() OVER (PARTITION BY license_number ORDER BY created_at DESC) as rn
  FROM drivers
  WHERE license_number IS NOT NULL
)
-- احفظ السجلات المحذوفة
INSERT INTO drivers_deleted_backup
SELECT d.*, NOW(), 'duplicate license_number'
FROM drivers d
JOIN duplicates dup ON d.driver_id = dup.driver_id
WHERE dup.rn > 1;

-- احذف التكرارات
DELETE FROM drivers
WHERE driver_id IN (
  SELECT driver_id
  FROM duplicates
  WHERE rn > 1
);

-- 2. حل تكرار لوحات الحافلات
WITH duplicates AS (
  SELECT 
    license_plate,
    bus_id,
    created_at,
    ROW_NUMBER() OVER (PARTITION BY license_plate ORDER BY created_at DESC) as rn
  FROM buses
  WHERE license_plate IS NOT NULL
)
INSERT INTO buses_deleted_backup
SELECT b.*, NOW(), 'duplicate license_plate'
FROM buses b
JOIN duplicates dup ON b.bus_id = dup.bus_id
WHERE dup.rn > 1;

DELETE FROM buses
WHERE bus_id IN (
  SELECT bus_id
  FROM duplicates
  WHERE rn > 1
);

-- 3. حل تكرار المقاعد
WITH duplicates AS (
  SELECT 
    bus_id,
    seat_number,
    seat_id,
    created_at,
    ROW_NUMBER() OVER (PARTITION BY bus_id, seat_number ORDER BY created_at DESC) as rn
  FROM seats
)
INSERT INTO seats_deleted_backup
SELECT s.*, NOW(), 'duplicate seat_number for bus'
FROM seats s
JOIN duplicates dup ON s.seat_id = dup.seat_id
WHERE dup.rn > 1;

DELETE FROM seats
WHERE seat_id IN (
  SELECT seat_id
  FROM duplicates
  WHERE rn > 1
);

COMMIT;
```

---

## 4. التحقق من صحة البيانات

### 4.1 فحص القيم غير الصالحة

```sql
-- ملف: analysis/003_validate_data.sql

-- 1. أسعار سالبة أو صفر
SELECT 'Negative/Zero Prices' as issue, COUNT(*) as count
FROM bookings
WHERE total_price <= 0;

-- 2. أوقات رحلات غير منطقية
SELECT 'Invalid Trip Times' as issue, COUNT(*) as count
FROM trips
WHERE arrival_time <= departure_time;

-- 3. سعة حافلات غير منطقية
SELECT 'Invalid Bus Capacity' as issue, COUNT(*) as count
FROM buses
WHERE capacity <= 0 OR capacity > 100;

-- 4. تقييمات خارج النطاق
SELECT 'Invalid Ratings' as issue, COUNT(*) as count
FROM ratings
WHERE stars < 1 OR stars > 5;

-- 5. نسب استرداد غير صالحة
SELECT 'Invalid Refund Percentage' as issue, COUNT(*) as count
FROM cancel_policies
WHERE refund_percentage < 0 OR refund_percentage > 100;

-- 6. نسب عمولة غير صالحة
SELECT 'Invalid Commission Percentage' as issue, COUNT(*) as count
FROM partners
WHERE commission_percentage < 0 OR commission_percentage > 100;
```

### 4.2 إصلاح القيم غير الصالحة

```sql
-- ملف: cleanup/003_fix_invalid_data.sql

BEGIN;

-- 1. إصلاح الأسعار السالبة (تحويلها لقيمة مطلقة)
UPDATE bookings
SET total_price = ABS(total_price)
WHERE total_price < 0;

-- 2. إصلاح أوقات الرحلات (إضافة ساعة للوصول)
UPDATE trips
SET arrival_time = departure_time + INTERVAL '1 hour'
WHERE arrival_time <= departure_time;

-- 3. إصلاح سعة الحافلات (تعيين قيمة افتراضية)
UPDATE buses
SET capacity = 40
WHERE capacity <= 0 OR capacity > 100;

-- 4. إصلاح التقييمات (تقريب للنطاق الصالح)
UPDATE ratings
SET stars = GREATEST(1, LEAST(5, stars));

-- 5. إصلاح نسب الاسترداد
UPDATE cancel_policies
SET refund_percentage = GREATEST(0, LEAST(100, refund_percentage));

-- 6. إصلاح نسب العمولة
UPDATE partners
SET commission_percentage = GREATEST(0, LEAST(100, commission_percentage));

COMMIT;
```

---

## 5. التحقق النهائي

### 5.1 اختبار الجاهزية

```sql
-- ملف: tests/001_pre_migration_readiness.sql

-- اختبار شامل قبل المرحلة 1
DO $$
DECLARE
  orphan_count INTEGER;
  duplicate_count INTEGER;
  invalid_count INTEGER;
BEGIN
  -- 1. فحص البيانات اليتيمة
  SELECT COUNT(*) INTO orphan_count
  FROM passengers p
  LEFT JOIN bookings b ON p.booking_id = b.booking_id
  WHERE b.booking_id IS NULL;
  
  IF orphan_count > 0 THEN
    RAISE EXCEPTION 'Found % orphaned passengers - cleanup required', orphan_count;
  END IF;
  
  -- 2. فحص التكرارات
  SELECT COUNT(*) INTO duplicate_count
  FROM (
    SELECT license_number
    FROM drivers
    WHERE license_number IS NOT NULL
    GROUP BY license_number
    HAVING COUNT(*) > 1
  ) x;
  
  IF duplicate_count > 0 THEN
    RAISE EXCEPTION 'Found % duplicate driver licenses - cleanup required', duplicate_count;
  END IF;
  
  -- 3. فحص القيم غير الصالحة
  SELECT COUNT(*) INTO invalid_count
  FROM bookings
  WHERE total_price <= 0;
  
  IF invalid_count > 0 THEN
    RAISE EXCEPTION 'Found % bookings with invalid prices - cleanup required', invalid_count;
  END IF;
  
  RAISE NOTICE '✅ All pre-migration checks passed! Ready for Phase 1.';
END $$;
```

---

## 6. التوثيق والتقرير

### 6.1 تقرير التنظيف النهائي

```sql
-- إنشاء تقرير شامل
SELECT 
  issue_type,
  records_count,
  severity,
  action_taken,
  deleted_records,
  fixed_records,
  created_at
FROM data_cleanup_log
ORDER BY created_at DESC;

-- إحصائيات عامة
SELECT 
  COUNT(*) as total_issues,
  SUM(deleted_records) as total_deleted,
  SUM(fixed_records) as total_fixed
FROM data_cleanup_log;
```

---

## ✅ معايير الاكتمال

- [ ] جميع البيانات اليتيمة محددة وموثقة
- [ ] البيانات اليتيمة الحرجة محذوفة أو مصلحة
- [ ] البيانات المالية اليتيمة مراجعة يدوياً
- [ ] جميع التكرارات محلولة
- [ ] جميع القيم غير الصالحة مصلحة
- [ ] اختبار الجاهزية النهائي ناجح
- [ ] تقرير التنظيف موثق ومحفوظ
- [ ] نسخ احتياطية من البيانات المحذوفة محفوظة

---

## ⚠️ تحذيرات مهمة

1. **لا تحذف البيانات المالية تلقائياً** - راجع يدوياً دائماً
2. **احفظ نسخ احتياطية** من كل البيانات المحذوفة
3. **وثق كل إجراء** في `data_cleanup_log`
4. **اختبر على staging أولاً** قبل الإنتاج
5. **احصل على موافقة** على حذف البيانات من الإدارة
